/* Staff-side validation bridge disabled.
   app.js + staff-view.js own the realtime UI and event handling.
   Keeping this file inert prevents duplicate click handlers and page reloads. */
(() => {})();
